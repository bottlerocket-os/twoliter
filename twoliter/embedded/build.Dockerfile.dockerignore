# Exclude everything from the build context by default.
*

# Include the specific files expected by the Dockerfile.
!/build/tools
!/packages/*/*.spec
!/Twoliter.toml

# Include .cargo to support offline builds with cached crates.
!/.cargo

# Include everything under sources.
!/sources

# Exclude "target" directories everywhere.
*/target/*
