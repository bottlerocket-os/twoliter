/*!
Intentionally empty stub used as the `lib.rs` for every crate in the
`guest-images-graph` fixture. This fixture exists only to drive Cargo metadata
for unit tests of buildsys's manifest graph logic — it is not built by
`twoliter build`.
!*/
